export const firebase = (state) => state.firebase.firebase; 

export const userAdminOfficeList = (state) => state.auth.adminOfficeList;

export const rehydrated = (state) => state._persist.rehydrated;